# Frontend

Web client for the education platform lives here.

The backend currently serves approved materials and quizzes from `docs/materials/`.
Scaffold the React (or React Native web) app in this folder when the API slice is ready to consume.
